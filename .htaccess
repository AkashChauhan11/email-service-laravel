<IfModule mod_rewrite.c>
    RewriteEngine On

    # If hosting points to project root (not /public), send all requests to /public.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]

</IfModule>
